name: AlfredIndicators CI

on:
  push:
    branches: [ Main ]
  pull_request:
    branches: [ Main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Compile MQL4 Indicators
        run: |
          echo "Compiling indicators..."
          # Add your compiler script or call MetaEditor CLI here
